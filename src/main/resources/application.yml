  spring:
    application:
      name: dine-in-order-api

#DATASTRUCTRE CONFIGRATION
    datasource:
      url: ${MYSQL_URL}
      username: ${MYSQL_USERNAME}
      password: ${MYSQL_PASSWORD}
    jpa:
      show-sql: true
      hibernate:
        ddl-auto: update


  app:
    base-url: /api/v1
    domain:
      name: ${DOMAIN_NAME}
      secure: ${DOMAIN_SECURE}
      same-site: ${DOMAIN_SAME_SITE}

    cloudinary:
      cloud-name: ${cloud_name}
      api-key: ${api_key}
      api-secret: ${api_secret}

    security:
      public-endpoints:
        - /register
        - /login
        - /restaurant/{restaurantId}/food-items

      secret: ${SECURITY_SECRET}
      token_validity:
        access-validity: 3600
        refresh-validity: 1296000




